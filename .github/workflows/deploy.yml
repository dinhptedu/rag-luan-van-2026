name: Deploy to Hugging Face Space

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Push to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "you@example.com"
          git config --global user.name "Dinhpt"

          git clone https://Dinhpt:$HF_TOKEN@huggingface.co/spaces/Dinhpt/rag-luan-van-2026 space

          rsync -av --exclude='.git' ./ space/

          cd space
          git add .
          git commit -m "auto deploy" || echo "no changes"

          git push https://Dinhpt:$HF_TOKEN@huggingface.co/spaces/Dinhpt/rag-luan-van-2026 main
